import { MedicalService } from './medical-service';

describe('MedicalService', () => {
  it('should create an instance', () => {
    // @ts-ignore
    expect(new MedicalService()).toBeTruthy();
  });
});
